services:
  - type: web
    name: nutriguardfis
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run_webapp.py --host 0.0.0.0 --port $PORT
    autoDeploy: true
